{
  "name": "attendance-platform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "db:start": "docker-compose -f docker-compose.postgres.yml up -d",
    "db:stop": "docker-compose -f docker-compose.postgres.yml down",
    "db:reset": "docker-compose -f docker-compose.postgres.yml down -v && docker-compose -f docker-compose.postgres.yml up -d",
    "db:logs": "docker-compose -f docker-compose.postgres.yml logs -f postgres",
    "setup:db": "pnpm run db:start && sleep 10 && pnpm --filter auth-service run db:create",
    "setup:full": "pnpm install && pnpm run setup:db && pnpm run build && pnpm --filter auth-service run seed:roles && pnpm --filter auth-service run seed:institucional"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.27.0",
  "devDependencies": {
    "@types/node": "^22.19.6",
    "turbo": "^2.7.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.12",
    "@nestjs/core": "^11.1.12",
    "@nestjs/platform-express": "^11.1.11",
    "xlsx": "^0.18.5"
  }
}
